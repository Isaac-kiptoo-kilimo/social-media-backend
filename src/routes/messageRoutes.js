import {Router} from 'express';

const messageRouter=Router();

messageRouter.post('/messages', )

messageRouter.get('/messages', )

messageRouter.get('/messages/single/:MessageID',)

messageRouter.put('/messages/update/:MessageID',)

messageRouter.patch('/messages/patch/:MessageID',)

messageRouter.delete('/messages/delete/:MessageID',)


export default photoRouter;