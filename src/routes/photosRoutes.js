import {Router} from 'express';

const photoRouter=Router();

photoRouter.post('/photos', )

photoRouter.get('/photos', )

photoRouter.get('/photos/single/:UserID',)

photoRouter.put('/photos/update/:UserID',)

photoRouter.patch('/photos/patch/:UserID',)

photoRouter.delete('/photos/delete/:UserID',)


export default photoRouter;